import React from "react";

const page = () => {
  return (
    <div className="bg-copy-primary w-34 h-3/4">
      {/* <div className="flex gap-4">
        {themes.map((item, index) => (
          <button
            key={index}
            className="px-4 py-2 bg-background border rounded"
            onClick={() => toggleTheme(item)}
          >
            {item}
          </button>
        ))}
      </div> */}
      <h1 className="text-copy-primary"> page</h1>
    </div>
  );
};

export default page;
